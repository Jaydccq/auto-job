# Tech Debt Tracker

Durable record of known gaps that should not live only in chat.

| Date | Area | Gap | Impact | Suggested Fix | Status |
|------|------|-----|--------|---------------|--------|
| 2026-05-02 | Tracker schema lacks URL column | `data/applications.md` rows are keyed by `(num, company, role)` only — no canonical URL field. Mark-applied promotion now uses report-number to co-promote duplicate rows, but cross-listing detection still relies on heuristic company+role overlap. Adding URL would let `merge-tracker.mjs`, `dedupe-tracker-rows.mjs`, and the dashboard reason about job identity directly. | Manual review still required when two distinct requisitions share a company+role. | Add a 10th column (URL) to the markdown table. Migrate existing rows by reading the URL from the linked report's `**URL:**` header. Update `merge-tracker.mjs` and `dedupe-tracker-rows.mjs` to prefer URL match over company+role. Schema migration is high-blast-radius; defer until next batch of tracker changes. | Open |
| 2026-05-02 | Cross-requisition false negative | `npm run verify` warns about Anduril rows #3/#8 (different report numbers, same company+role). They are likely the same posting cross-listed at different times. The new `dedupe-tracker-rows.mjs` intentionally does NOT collapse these per Codex review (the heuristic could co-promote "Software Engineer I"/"II"). | A handful of pre-canonical duplicates remain in the tracker. | Surface these in the dashboard "review" pile with both rows side-by-side; let the user accept or reject the merge interactively. Or add an explicit `mergeWith` notes-field convention. | Open |
| 2026-04-21 | `batch/batch-runner.sh` | Report-number discovery loops over `reports/*.md` and assumes every basename begins with a numeric prefix. Non-report markdown files such as `reports/CLAUDE.md` can make arithmetic expansion fail. | Batch workers can start with an empty or corrupt report number, producing duplicate or malformed reports and pending tracker-addition TSVs. | Restrict report-number parsing to filenames matching `^[0-9]+-.*\\.md$`, add a shell test fixture with `reports/CLAUDE.md`, and fail before spawning workers if no report number can be reserved. | Open |
| 2026-04-22 | Bridge Vitest timeout config | The batch-runner e2e tests and `src/server.test.ts` background-adapter failure test can exceed Vitest's default 5s timeout during `npm run verify`; the same failed tests passed with `--testTimeout=20000`. | Full pipeline verification can report a false red even when bridge behavior passes under a realistic timeout. | Set explicit per-test/file-level timeouts or a bridge test config timeout for the known slow tests, then keep `npm run verify` on the default command. | Open |
| 2026-04-23 | Scanner browser lifecycle | Newgrad and LinkedIn scan flows rely on different browser runtimes (`newgrad-scan` uses Playwright persistent context; `linkedin-scan` uses bb-browser). Cleanup behavior is only partially surfaced in operator output, and stale browser state can still be confused with active scan tabs. | Operators can end scans with cluttered browser state, stale login assumptions, or uncertainty about whether LinkedIn/JobRight sessions are available. | Add shared scanner preflight/postflight diagnostics that list the browser runtime in use, verify login state for the selected source, close only tabs opened by the current run, and report orphaned browser processes. | Partially fixed for `newgrad-scan`: default headless bundled Chromium, bridge preflight before browser launch, scan browser closes after detail enrichment, and an old orphaned Playwright browser was cleaned up. |
| 2026-04-23 | Historical JD cache hygiene | Older ignored `jds/*.txt` files still contain stale JobRight shell text such as `Represents the skills you have` and fake `Turbo for Students` salary frontmatter. The live extractor and quick-eval sanitizer now guard against this for new inputs, but old cache files remain on disk. | If an old unchecked pipeline row is evaluated from local cache, the worker may see misleading salary/description metadata even though structured Requirements/Responsibilities are usually present. | Add a cache migration or audit script that rewrites/removes low-value description and fake salary fields from existing `jds/*.txt`, then run it once and add a regression check for ignored cache hygiene if the files remain locally important. | Open |
| 2026-04-23 | JobRight skill tag cleanup | Current newgrad enrich output can still include low-value JobRight metadata inside `Skill tags`, such as applicant counts, company-stage labels, sponsorship badges, and promotional fragments like `94% OFF`. The main JD description, requirements, responsibilities, salary, and URLs were clean in the 2026-04-23 run. | The noise is lower risk than shell text in `Description excerpt`, but it can still distract evaluations or inflate perceived signal if prompt consumers treat every skill tag as a real requirement. | Normalize JobRight skill tags before writing JD cache/evaluation payloads: keep concrete technologies, domains, and sponsorship tags; drop applicant counts, company-stage labels, promotional text, and duplicate newline-expanded tag blocks. Add a focused regression fixture for current `jds/r1-rcm-4dd6510d.txt`-style output. | Open |
| 2026-04-24 | LinkedIn detail extraction | Live `linkedin-scan` list extraction logs clean title/company/location/URL rows, but detail enrichment previously produced `detailTextChars=0` for LinkedIn job-view rows even when canonical job URLs were valid. | Detail scoring could reject otherwise relevant LinkedIn jobs because the JD body, requirements, and responsibilities were missing from the enriched payload. | Strengthen LinkedIn detail-pane extraction and/or guest `jobPosting` fallback for current job-view page states; add a fixture that fails when detail text is empty for a known public LinkedIn job URL shape. | Fixed for guest `jobPosting` fallback: DeepIntent live verification enriched 6,379 chars, 12 requirements, and 12 responsibilities. Keep monitoring authenticated detail-pane extraction. |
| 2026-04-28 | Desktop Electron dev loop | `npm --prefix apps/desktop run dev` failed in the `tsx` Electron loader path with `ERR_INTERNAL_ASSERTION`; this was already present in parent desktop commit `b691a3f` and was not a Stage 6 regression. | Desktop iteration through `dev` was blocked; packaged app validation still worked through `package:dir` and the built `.app`. | Replaced the dev/start commands with compile-first Electron launches from `dist/main.js`, avoiding the failing `tsx` loader path. | Fixed |
| 2026-04-28 | Desktop icon artwork | `apps/desktop/icons/icon.icns`, `tray.png`, and `trayTemplate.png` were placeholder assets; the tray PNGs were 16x16 black-ring images. | The packaged app was functional but visually unfinished in the menu bar and app bundle. | Replaced placeholder assets with GPT-generated artwork, added `icon.png` as the repo-local source asset, regenerated `icon.icns`, and switched tray loading to the color tray PNG. | Fixed |
| 2026-04-28 | Gmail rejection phrase coverage | `HARD_REJECTION_PATTERNS` (Phase 1 Task 5) does not yet cover common ATS templates `we regret to inform you`, `moving in a different direction`, `not (be )?advancing`, `position has been put on hold`. `SOFT_REJECTION_PATTERNS` hiring-noun guard misses `opportunity` and `opening`. | Some rejection emails will silently land in `applied` or fall through entirely instead of being classified as `rejected`, polluting the active-tracker view. | Added the missing hard phrases (`regret to inform`, `moving in a different direction`, `will not(?: be)? advancing` / `we are not advancing`, `position has been put on hold`, `role has been put on hold`). Promoted the inline hiring-noun regex to a named `HIRING_NOUN_PATTERN` covering `application` / `candidacy` / `interview` / `role` / `position` / `opportunity` / `opening` / `requisition`. Hard-rejection presence now also satisfies the upstream hiring-context gate so terse rejection emails without other hiring context still classify. | Fixed |
| 2026-04-28 | Gmail confidence-feature wiring | `senderClassificationPolicy(from)` is computed twice per message in `scripts/gmail-oauth-refresh.mjs` — once inside `classifyEvent` and once in `extractSignalFromMessage` (Phase 1 Task 6). Two inline regexes in `extractSignalFromMessage`'s confidence-feature block (`/\b(offer letter\|job offer\|interview is scheduled\|online assessment)\b/i` and `/\b(application update\|interview\|offer\|assessment)\b/i`) are unnamed one-offs. The Task 6 `fakeAtsMessage` test fixture exercises every feature simultaneously, producing `confidence === 1.00` and not validating gradient behavior. | Minor cosmetic / micro-perf cost; gradient bug could go unnoticed because no test fixture lands in the 0.6–0.9 mid-range. | `classifyEvent` now accepts an optional pre-computed policy; `extractSignalFromMessage` computes it once and threads it through. Promoted both inline regexes to module-scope `HARD_EVENT_PHRASE_PATTERN` and `SUBJECT_MATCH_PATTERN`. Added `fakeNonAtsMessage` fixture and a gradient test asserting non-ATS senders produce confidence < 0.85 (the all-features-true ceiling). | Fixed |
| 2026-04-29 | Gmail sender taxonomy externalization (Phase 5) | `TRUSTED_RECRUITING_DOMAINS`, `RESTRICTED_SENDER_DOMAINS`, `GENERIC_SENDER_NAMES`, and `ATS_SENDER_NAME_SUFFIX_RE` are hardcoded module-level constants in `scripts/gmail-oauth-refresh.mjs`. The original Phase 5 plan was to externalize these to `config/gmail-senders.yml` so users could add ATS domains without touching code. | Adding a new ATS or denying a misbehaving sender requires editing the scanner script. Low impact — the lists rarely change. | Create `config/gmail-senders.example.yml` with the current values, write a `scripts/gmail-senders-config.mjs` loader that prefers the user file and falls back to defaults, plumb through `senderClassificationPolicy` and `companyFromAtsSenderName`. Add tests that load both the example and a synthetic override. Deferred — explicitly approved by the user as quality-improvement work that can wait behind Phase 4 deadline parsing. | Open |
| 2026-04-29 | Scan launcher — live UI badge during a run | `web/template.html` scan launcher refreshes the last-run badge only after the SSE stream ends + `loadScanCatalog()` is rerun. During an in-flight scan the cards keep showing the previous status. Acceptable for a local-first single-user app — the in-memory registry is authoritative on the post-end refresh, so the green/red dot eventually arrives without manual reload. | Cosmetic; status feels stale during long scans (~minutes). | Add a polling tick in `loadScanCatalog()` while `#scan-runner-busy` is visible (every ~3s), or push the catalog delta into the existing SSE stream. Defer until the launcher gets its second user. | Open |
| 2026-04-29 | Scan launcher — duplicated `resolveWebDir` | `apps/server/src/server.ts` has a `resolveWebDirForScans` helper that mirrors `resolveWebDir` in `apps/server/src/routes/dashboard.ts`. The duplication exists because the scan-launcher dynamic `import()` of `web/dashboard-handlers.mjs` needs the same env-override → `<repoRoot>/web` → source-tree fallback chain, but `routes/dashboard.ts` doesn't export it. | Two copies of identical logic; future packaging changes must touch both. | Extract to `apps/server/src/lib/web-dir.ts`, import from both call sites. ~15 lines, no test changes needed since both call sites are already covered. | Open |
| 2026-04-29 | Gmail deadline parsing coverage (Phase 4) | `parseDeadline` covers ISO dates, "in N days/weeks", and "by Month Day [, Year]" — but real ATS interview emails frequently use phrasings like "schedule by EOD Friday", "next Monday", "tomorrow at 3pm PT", or relative weekday names with timezone. Initial replay against the user's 295-signal corpus produced 0 deadlines because the stored 220-char snippets typically don't contain explicit dates (they got truncated, OR the schedule lives in an external link). The scanner-side path uses the full 4000-char body, so live scans will produce more hits — but coverage of weekday/EOD phrasings is still missing. | Some interview/OA messages with implicit deadlines won't promote to urgent attention. | Extended `parseDeadline` with `tomorrow`, `end of week` (→ upcoming Friday), and weekday phrasings (`by Friday`, `by next Friday`, `by EOD Monday`, `before Tuesday`, etc.). `next Friday` correctly skips one week ahead vs. `Friday` which targets the upcoming occurrence. Same-day match when reference falls on the named weekday. Timezone-aware time-of-day parsing (`tomorrow at 3pm PT`) is still out of scope — log if it shows up frequently in the corpus. | Fixed |
| 2026-05-02 | LinkedIn Apply popup discovery race coverage | `probeExternalApplyUrl` in `scripts/linkedin-scan-bb-browser.ts` now uses a quiet-window discovery loop (1.2s quiet → bail; 6s ceiling) rather than breaking on the first new tab. Behavior is empirical from the codex review and not yet covered by a unit test. `listBbTabs()` itself can hang and falls back to `runBb`'s 120s timeout. | Real-world bug fix is in place; lack of regression test means future refactors could re-introduce the early-break bug. | Extract the discovery loop into a pure helper that takes an injectable `listTabsFn`, add vitest cases for "popup arrives at t=2.5s", "two popups arrive 800ms apart", "no popup ever". Tighten the inner `runBb` timeout to ~3× the discovery window for `tab list` calls only. | Open |
| 2026-05-02 | Easy Apply slow-`interactive` ATS confirms only on timeout | `settleFinalUrl` confirms the URL once `stableMs` passes but only RETURNS it early when `readyState === "complete"`. A site stuck on `interactive` will only return at `maxMs` (12s). Functionally correct; user-visible delay. | Slower `linkedin-scan` enrichment for the rare site that never reports `complete`. | Add an `acceptInteractiveAfterMs` option (e.g. 6s) so we accept `interactive` once stableMs has been met for long enough. Already keyword-gated in the helper — small change. | Open |
| 2026-05-02 | Easy Apply iframe-hosted forms | `findEasyApplyModal` only inspects same-origin DOM. Some Easy Apply variants embed third-party widgets in iframes, which the modal-scoped scan cannot read or fill. | Auto-fill silently no-ops on those steps; user falls back to manual fill. Rare in practice. | Detect iframe presence inside the modal and surface a clear "iframe step — fill manually" message rather than a misleading "Nothing to fill on this step". | Open |
