{
  "name": "@auto-job/email-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Gmail-driven verification-link clicker for ATS account challenges. Existing accounts only; new-account flows are Phase 4.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "@auto-job/browser": "workspace:*",
    "@auto-job/humanize": "workspace:*",
    "yaml": "^2.8.3"
  }
}
