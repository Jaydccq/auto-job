{
  "name": "@auto-job/extension",
  "version": "0.1.0-phase1",
  "private": true,
  "description": "Chrome Manifest V3 extension popup for auto-job. Phase 1 = contracts only.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.332",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@auto-job/shared": "workspace:*"
  }
}
